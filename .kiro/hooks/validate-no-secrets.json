{
  "version": "v1",
  "hooks": [
    {
      "name": "Validate No Secrets in Prompt",
      "trigger": "PreToolUse",
      "description": "Pre-write validation to prevent secrets from being committed to source",
      "matcher": "fs_write|fs_append",
      "action": {
        "type": "agent",
        "prompt": "Before writing files, verify: No hardcoded secrets, API keys, private keys, or credentials are being written to source code. All sensitive values must come from environment variables."
      }
    }
  ]
}